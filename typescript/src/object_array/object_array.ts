let names = ['aaa','bbbb','ccc'];
//names.push(10); // not work
names.push('dddd');

let numbers = [1,2,3,4];

numbers.push(10); 
//numbers.push('dddd'); // not work
let mixArray = ['aaa',1,2,'bbb'] // here we can add both string and numbers


let obj = {
    "name" : "test",
    "age" : 10,
    "status" : true
}

// we cannot define this obj.address